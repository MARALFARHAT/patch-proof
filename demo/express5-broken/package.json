{
  "name": "patchproof-express5-fixture",
  "version": "1.0.0",
  "private": true,
  "description": "A deliberately broken Express 5 repository for the PatchProof migration-agent demo.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test --test-concurrency=1"
  },
  "dependencies": {
    "express": "5.2.1"
  }
}

